import React from "react";
import "./Title.css";

const Title = props => (
    <div className="titlebox">
        <h1>Soccer Clicky Game!</h1>
        <p>Click on an image of soccer players to earn points, but don't click on any of them than once!</p>
    </div>
);
export default Title;